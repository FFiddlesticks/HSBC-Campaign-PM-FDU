# 核心功能依赖
pdf2image>=1.17.0
poppler-utils  # pdf2image的系统依赖
dashscope>=1.10.0
img2pdf>=0.4.4

# 数据处理（用于测试）
pandas>=2.0.0
openpyxl>=3.1.0  # pandas读取xlsx文件需要

# 配置管理
pydantic-settings>=2.0.0
pydantic>=2.0.0

# Web框架（可选，保持与原项目兼容）
fastapi>=0.100.0,<1.0.0
uvicorn>=0.22.0,<1.0.0
python-multipart>=0.0.6,<0.0.7

# 图像处理（pdf2image可能需要）
Pillow>=9.0.0

# 环境变量管理
python-dotenv>=1.0.0